#include <stdio.h>
#include <math.h>    
    int main()
{
    int l1, l2, hip, soma;
    
        printf("entre com l1: ");
            scanf("%d", &l1);
        printf("entre com l2: ");
            scanf("%d", &l2);
            
        soma = pow(l1,2) + pow(l2,2);
        hip = sqrt(soma);
        
        printf("hip");
}    
    